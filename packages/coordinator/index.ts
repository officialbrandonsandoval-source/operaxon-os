/**
 * @operaxon/coordinator — PROPRIETARY
 *
 * Coordinator is the multi-agent orchestration layer of Operaxon OS.
 * It enables agents to spawn, delegate to, and communicate with other agents.
 *
 * Capabilities:
 * - Agent spawning and lifecycle management
 * - Task delegation and result routing
 * - Agent-to-agent messaging
 * - Parallel task execution with dependency resolution
 * - Agent registry and capability discovery
 * - Deadlock detection and recovery
 *
 * This package is proprietary and not included in the open-source runtime.
 * Contact team@operaxon.com for licensing.
 */

export class Coordinator {
  constructor() {
    throw new Error(
      'Coordinator is a proprietary Operaxon OS component. ' +
      'See https://operaxon.com/enterprise for access.'
    );
  }
}
