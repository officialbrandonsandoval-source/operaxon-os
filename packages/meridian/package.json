{
  "name": "@operaxon/meridian",
  "version": "0.1.0",
  "description": "Meridian — memory consolidation engine (Dream Engine) for Operaxon OS",
  "license": "SEE LICENSE IN LICENSE-PROPRIETARY",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test dist/**/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@operaxon/types": "*",
    "@operaxon/security": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
