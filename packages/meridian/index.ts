/**
 * @operaxon/meridian — PROPRIETARY
 *
 * Meridian is the memory and knowledge management layer of Operaxon OS.
 * It gives agents persistent, structured, queryable memory across sessions.
 *
 * Capabilities:
 * - Long-term memory store with semantic search
 * - Episodic memory (session history)
 * - Procedural memory (learned patterns)
 * - Memory consolidation and pruning
 * - Cross-agent shared memory with access control
 *
 * This package is proprietary and not included in the open-source runtime.
 * Contact team@operaxon.com for licensing.
 */

export class Meridian {
  constructor() {
    throw new Error(
      'Meridian is a proprietary Operaxon OS component. ' +
      'See https://operaxon.com/enterprise for access.'
    );
  }
}
