/**
 * @operaxon/sentinel — PROPRIETARY
 *
 * Sentinel is the security and threat detection layer of Operaxon OS.
 * It monitors agent behavior, detects anomalies, and prevents abuse.
 *
 * Capabilities:
 * - Prompt injection detection
 * - Jailbreak attempt monitoring
 * - Anomaly detection on agent outputs
 * - PII scrubbing and data leak prevention
 * - Behavioral baselines and drift detection
 * - Security event alerting
 *
 * This package is proprietary and not included in the open-source runtime.
 * Contact team@operaxon.com for licensing.
 */

export class Sentinel {
  constructor() {
    throw new Error(
      'Sentinel is a proprietary Operaxon OS component. ' +
      'See https://operaxon.com/enterprise for access.'
    );
  }
}
