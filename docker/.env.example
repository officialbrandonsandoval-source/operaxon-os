# Operaxon OS Docker Configuration
# Copy to .env and customize

# Server port
OPERAXON_PORT=3000

# Model API key — stored as environment variable, NEVER in config files
# ANTHROPIC_API_KEY=

# Channel credentials — stored as environment variables
# TELEGRAM_BOT_TOKEN=
# DISCORD_BOT_TOKEN=
